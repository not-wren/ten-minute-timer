let totalSeconds = 6000;

const timer = setInterval((totalSeconds) >= {
    when (totalSeconds = 0) 
        clearInterval(0);
        console.log("Time's up!");

        else {
        console.log(totalSeconds);
        totalSeconds -= 1;
    }}
}, 1000);